import { Routes } from '@angular/router';
import { LottieAnimationComponent } from './lottie-animation/lottie-animation.component';

export const routes: Routes = [
    { path: '', component: LottieAnimationComponent },
];
